var Conway = (function() {
  
  function Conway(width, height) {
    var width = width, height = height, data = new Array2(width, height);
    
    this.step = function() {
    };
  };
  
  return Conway;
} ());
